/**
 * Type definitions for bash-orchestrator module
 */

import { BaseEntity } from '../shared/types';

// TODO: [FEATURE:bash-orchestrator] Define feature-specific types

export interface Bash-orchestratorConfig {
  // Configuration options
}

export interface Bash-orchestratorEntity extends BaseEntity {
  // Entity fields
}
