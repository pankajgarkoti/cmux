/**
 * Type definitions for agent-system module
 */

import { BaseEntity } from '../shared/types';

// TODO: [FEATURE:agent-system] Define feature-specific types

export interface Agent-systemConfig {
  // Configuration options
}

export interface Agent-systemEntity extends BaseEntity {
  // Entity fields
}
