/**
 * Type definitions for fastapi-server module
 */

import { BaseEntity } from '../shared/types';

// TODO: [FEATURE:fastapi-server] Define feature-specific types

export interface Fastapi-serverConfig {
  // Configuration options
}

export interface Fastapi-serverEntity extends BaseEntity {
  // Entity fields
}
